import React from "react";
import RolePageTemplate from "./RolePageTemplate.jsx";

export default function LibraryPlaceholderPage({ title, description }) {
  return <RolePageTemplate title={title} description={description} />;
}
